var favs = document.querySelectorAll('.fa-heart')

for (let fav in favs ){
   fav.addEventlinstnner (" click", function(){
       if (faw.style.color == 'black'){
        faw.style.color == 'red'
       }
   })
}
   
for (let fav in favs ){
    fav.addEventlinstnner (" click", function(){
        if (faw.style.color == 'black'){
         faw.style.color == 'red'
        }
    })
 }

 for (let i  of deleteBtn) {
    i.addEventlinstnner("click",function()){
        deleteBtn [i].remove()
    }
 }

 for (let incer  of plus ) {
    i.addEventlinstnner("click",function()){
        deleteBtn [i].add()
    }
 }